<div class="row">
    <div class="col-auto">
        <h2>Welcome, {{first_name}}, {{last_name}} </h2>
    </div>
</div>

<div class="col-md-6">
    <h3>Employee Availability</h3>

    {{#each employee as |employee|}}
    <div class="row mb-2">
        <div class="col-md-8">
            <h4><a href="/Employee/{{employee.id}}">{{unavailability}}{{task.id}}</a></h4>
        </div>
        <div class="col-md-4">

        </div>
        {{/each}}
    </div>